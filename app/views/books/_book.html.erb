<div class="flex rounded shadow-lg m-5 bg-gray-100">
  <div>
    <%= image_tag book.cover.variant(resize: '100x150!') %>
  </div>
  <div class="px-6 py-4">
    <div class="font-bold text-xl mb-2"><%= book.title %></div>
    <p class="text-gray-700 text-base"><%= truncate(book.description, length: 70) %></p>
    <%= book_addition_button(book) %>
  </div>
</div>
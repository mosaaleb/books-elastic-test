<div class="max-w-sm rounded shadow-lg m-5">
  <div>
    <%= image_tag book.cover.variant(resize: '100') %>
  </div>
  <div class="px-6 py-4">
    <div class="font-bold text-xl mb-2"><%= book.title %></div>
    <p class="text-gray-700 text-base"><%= truncate(book.description, length: 40) %></p>
  </div>
</div>
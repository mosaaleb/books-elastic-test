<div class="flex flex-colborder w-1/2 bg-gray-200">
  <%= image_tag(book.cover) %>
  <p class="text-2xl"><%= book.title %></p>
  <p class="text-lg"><%= book.description %></p>
  <p class="text-md"><%= book.isbn %></p>
</div>